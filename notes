code 400: 
	URI goes before root
	bad character in version

code 414:
	URI too long






- check that the URI doesn't get less than the root with ..
- Header lines allow only specific character according to RFC
  ( # ! $ % & ' * + - | . backtick ^ _ ~ 0-9 a-z A-Z
- Header values can be capitalized or not (so convert to lowercase parsing)
- Are we handling a chunked body?
- 